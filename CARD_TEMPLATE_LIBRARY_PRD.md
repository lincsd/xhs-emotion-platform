# 🎴 小红书图文卡片模板库 — 产品设计文档

> Card Template Library — Product Requirements Document (PRD)  
> 版本: v1.0 · 2026-03-16  
> 定位: 独立功能模块，可嵌入主平台，也可单独使用

---

## 一、产品概述

### 1.1 是什么

**卡片模板库**是一个集「浏览 → 预览 → 选用 → 自建 → 分享」于一体的小红书图文卡片模板中心。

用户可以从 **50+ 预置模板** 中一键选用，也可以通过**可视化编辑器**零代码创建自定义模板，并导入/导出 JSON 与他人共享。

### 1.2 为什么要做

| 现状 | 问题 |
|------|------|
| 当前系统内置 15 个模板，硬编码在 `CardEngine.templates` 中 | 模板数量有限，用户无法自定义 |
| 模板风格集中在文艺/商务/治愈等基础风格 | 缺少小红书当前最流行的风格（便签风、报纸风、3D、Y2K等） |
| 新增模板需要开发者写代码 | 运营同学无法自助扩展模板 |
| 没有模板分类/搜索/收藏体系 | 模板少还行，模板多了就找不到想要的 |

### 1.3 目标用户

| 角色 | 需求 |
|------|------|
| 小红书博主（初级）| 一键选用好看的模板，不想折腾 |
| 小红书博主（进阶）| 自定义品牌色、字体、布局，打造个人风格 |
| 账号运营团队 | 统一团队视觉规范，多人共用同一套模板 |
| 设计师 | 制作模板并分享/售卖 |

### 1.4 核心指标

- **模板覆盖率**: 覆盖 90% 的小红书主流卡片风格
- **模板使用率**: 用户生成卡片时模板选用率 > 80%
- **自定义模板数**: 平均每用户创建 ≥ 2 个自定义模板
- **模板加载速度**: 模板列表渲染 < 200ms

---

## 二、模板分类体系

### 2.1 三维分类法

模板按三个维度分类，用户可组合筛选：

```
维度 A: 内容形式（做什么）
维度 B: 视觉风格（长什么样）
维度 C: 适用赛道（给谁用）
```

### 2.2 维度 A — 内容形式（16 类）

> 决定模板的「骨架」— 信息结构与布局方式

| # | 形式 ID | 名称 | 说明 | 页面结构 |
|---|---------|------|------|----------|
| 1 | `quote` | 语录金句卡 | 一句话为主体，大字居中或偏移，留白多 | 封面(=内容) |
| 2 | `listicle` | 清单列表卡 | 编号列表（Top 10、必备清单、推荐列表）| 封面 + 多页内容 |
| 3 | `tutorial` | 教程步骤卡 | 分步骤讲解，Step 1/2/3 格式 | 封面 + 步骤页 |
| 4 | `comparison` | 对比测评卡 | 左右/上下对比布局（产品PK、Before/After）| 封面 + 对比页 |
| 5 | `story` | 叙事长文卡 | 长篇图文故事，多页阅读 | 封面 + 多页正文 + 结尾 |
| 6 | `qa` | 问答互动卡 | 问题+回答格式，适合知识科普 | 封面 + 问答页 |
| 7 | `checklist` | 打卡清单卡 | 带勾选框的清单，适合习惯养成/旅行打卡 | 封面 + 清单页 |
| 8 | `ranking` | 排行榜卡 | 排名展示，Top N，带编号和评分 | 封面 + 排行页 |
| 9 | `timeline` | 时间线卡 | 时间轴布局，适合经历回顾/成长记录 | 封面 + 时间线页 |
| 10 | `recipe` | 食谱卡 | 食材+步骤结构，适合美食赛道 | 封面 + 食材页 + 步骤页 |
| 11 | `product` | 种草好物卡 | 产品展示+评价+价格，支持图片 | 封面 + 产品页 |
| 12 | `ootd` | 穿搭展示卡 | 照片为主+穿搭信息标注 | 封面 + 拼图页 |
| 13 | `travel` | 旅行攻略卡 | 景点介绍+路线+费用 | 封面 + 攻略页 |
| 14 | `review` | 评分测评卡 | 多维度评分（星级/雷达图）+文字评价 | 封面 + 评分页 |
| 15 | `poll` | 投票互动卡 | 选项展示，引导评论区互动 | 封面(=内容) |
| 16 | `daily` | 日签卡 | 日期+一句话+配图，适合每日更新 | 单页 |

### 2.3 维度 B — 视觉风格（20 类）

> 决定模板的「皮肤」— 配色、字体、装饰、纹理

| # | 风格 ID | 名称 | 关键词 | 主色调示例 |
|---|---------|------|--------|-----------|
| 1 | `minimal` | 极简留白 | 大量留白、细线条、低饱和 | `#2c2c2c` + `#f7f3ee` |
| 2 | `sweet` | 甜美清新 | 粉色系、圆角、可爱图标 | `#ff85a2` + `#fff5f8` |
| 3 | `morandi` | 莫兰迪色 | 低饱和、高级灰、柔和 | `#8d9f87` + `#f2ede8` |
| 4 | `cream` | 奶油风 | 米白、奶茶色、温暖柔和 | `#d4a574` + `#faf6f0` |
| 5 | `ins` | INS美学 | 胶片颗粒感、复古滤镜色 | `#c8a882` + `#f5e6d3` |
| 6 | `magazine` | 杂志排版 | 大标题、栅格、经典黑白 | `#1a1a1a` + `#ffffff` |
| 7 | `newspaper` | 报纸风 | 分栏排版、黑白、衬线字体 | `#333333` + `#f5f0e8` |
| 8 | `memo` | 便签风 | 手撕纸边缘、淡黄底、手写字 | `#5c4033` + `#fff8dc` |
| 9 | `letter` | 信纸风 | 横线底纹、信封元素、复古 | `#4a3728` + `#fdf5e6` |
| 10 | `handwrite` | 手写体 | 手绘感、涂鸦、随意排版 | `#333333` + `#fffef5` |
| 11 | `retro_chinese` | 新中式 | 中国红、传统纹样、书法 | `#c41e3a` + `#f5e6d0` |
| 12 | `y2k` | Y2K千禧 | 金属光泽、粉紫蓝渐变、未来感 | `#c77dff` + `#0a0a1a` |
| 13 | `dopamine` | 多巴胺色 | 高饱和撞色、活力、视觉冲击 | `#ff6b6b` + `#ffd93d` + `#6bcb77` |
| 14 | `acid` | 酸性设计 | 荧光绿/黄、扭曲字体、金属感 | `#c8ff00` + `#0a0a0a` |
| 15 | `cyber` | 赛博朋克 | 霓虹、深暗底、故障风 | `#00f5ff` + `#0a0a14` |
| 16 | `film` | 胶片复古 | 胶片边框、漏光、颗粒感 | `#e8d5b7` + `#2a2520` |
| 17 | `comic` | 漫画风 | 对话气泡、网点、粗线条 | `#ff4444` + `#ffffcc` |
| 18 | `pixel` | 像素风 | 像素字体、8bit色彩 | `#00ff00` + `#1a1a2e` |
| 19 | `glass` | 玻璃拟态 | 毛玻璃、渐变、投影、半透明 | `rgba(255,255,255,0.2)` + 渐变 |
| 20 | `gradient` | 渐变流体 | 大面积渐变、流体感、梦幻 | 紫粉蓝渐变 |

### 2.4 维度 C — 适用赛道（12 类）

> 决定模板推荐给谁

| # | 赛道 ID | 名称 | 典型关键词 |
|---|---------|------|-----------|
| 1 | `emotion` | 情感 | 治愈、语录、恋爱、成长 |
| 2 | `food` | 美食 | 探店、食谱、测评、零食 |
| 3 | `travel` | 旅行 | 攻略、打卡、酒店、路线 |
| 4 | `fitness` | 健身 | 减脂、塑形、食谱、打卡 |
| 5 | `fashion` | 穿搭 | OOTD、品牌、搭配、购物 |
| 6 | `tech` | 科技 | 数码、测评、教程、效率 |
| 7 | `ecommerce` | 电商 | 种草、好物、价格、对比 |
| 8 | `beauty` | 美妆 | 护肤、化妆、测评、色号 |
| 9 | `home` | 家居 | 装修、好物、收纳、DIY |
| 10 | `study` | 学习 | 考研、考公、英语、笔记 |
| 11 | `parent` | 母婴 | 育儿、辅食、教育、玩具 |
| 12 | `pet` | 萌宠 | 猫狗、养护、日常、用品 |

### 2.5 组合示例

```
语录金句 × 莫兰迪色 × 情感  → 「莫兰迪情感语录卡」
清单列表 × 便签风  × 美食  → 「便签风美食清单」
教程步骤 × 极简留白 × 学习  → 「极简学习笔记卡」
种草好物 × 奶油风  × 电商  → 「奶油风好物种草卡」
排行榜   × 赛博朋克 × 科技  → 「赛博科技排行榜」
```

---

## 三、模板数据模型

### 3.1 模板 JSON Schema（v2）

```jsonc
{
  // === 元信息 ===
  "id": "morandi_quote_001",           // 唯一ID
  "version": 2,                         // Schema 版本
  "name": "莫兰迪治愈语录",             // 显示名
  "nameEn": "Morandi Healing Quote",    // 英文名（可选）
  "icon": "🍃",                         // Emoji 图标
  "preview": "data:image/png;base64,…", // 缩略图预览（Base64 或 URL）
  "author": "system",                   // 作者: system | userName | community
  "createdAt": "2026-03-16T00:00:00Z",
  "updatedAt": "2026-03-16T00:00:00Z",
  "tags": ["莫兰迪", "高级感", "语录"],  // 自由标签，用于搜索
  "featured": true,                     // 是否精选推荐

  // === 三维分类 ===
  "category": {
    "form": "quote",                    // 内容形式（维度A）
    "style": "morandi",                 // 视觉风格（维度B）
    "tracks": ["emotion", "study"]      // 适用赛道（维度C，可多选）
  },

  // === 画布 ===
  "canvas": {
    "width": 1080,
    "height": 1440,
    "ratio": "3:4"                      // 支持 3:4 / 1:1 / 16:9
  },

  // === 配色方案 ===
  "palette": {
    "bg": "#f2ede8",                    // 主背景色
    "bgAlt": "#e8e0d6",                // 次背景色
    "card": "#ffffff",                  // 卡片区域色
    "text": "#4a4540",                  // 主文字色
    "textLight": "#8a8580",             // 次文字色
    "textMuted": "#b0a8a0",             // 弱文字色
    "accent": "#8d9f87",               // 强调色
    "accentAlt": "#a3b89d",             // 强调色2
    "border": "#d8d0c4",               // 边框色
    "shadow": "rgba(0,0,0,0.06)"       // 阴影色
  },

  // === 字体配置 ===
  "typography": {
    "titleFont": "bold 48px 'Noto Serif SC', serif",
    "bodyFont": "28px 'Noto Sans SC', sans-serif",
    "captionFont": "22px 'Noto Sans SC', sans-serif",
    "serif": true,                      // 标题用衬线体
    "italic": false,
    "titleSize": 48,
    "bodySize": 28,
    "lineHeight": 1.8,                  // 行高倍数
    "letterSpacing": 2                  // 字间距 px
  },

  // === 背景层（有序，从底到顶渲染）===
  "background": {
    "layers": [
      { "type": "solid", "color": "#f2ede8" },
      { "type": "texture", "texture": "paper_noise", "opacity": 0.03 },
      { "type": "gradient", "gradient": "radial", "colors": ["rgba(141,159,135,0.1)", "transparent"], "cx": 0.3, "cy": 0.2, "r": 0.6 }
    ]
  },

  // === 装饰元素（全局，每页都会绘制）===
  "decorations": [
    { "type": "border", "style": "double", "color": "#d8d0c4", "width": 2, "margin": 40, "radius": 0 },
    { "type": "corner_accent", "style": "line", "color": "#8d9f87", "size": 60, "positions": ["tl", "br"] }
  ],

  // === 页面类型定义 ===
  "pages": {
    "cover": {
      "padding": [200, 100, 180, 100],  // top, right, bottom, left
      "elements": [
        { "type": "tag", "y": 260, "font": "$caption", "color": "$accent", "border": true, "field": "category" },
        { "type": "title", "y": 450, "font": "$title", "color": "$text", "align": "center", "maxWidth": 860, "lineHeight": 72 },
        { "type": "divider", "y": 680, "width": 80, "color": "$accent", "thickness": 2 },
        { "type": "subtitle", "y": 740, "font": "$body", "color": "$textLight", "align": "center", "maxWidth": 700 },
        { "type": "author", "y": 1280, "font": "$caption", "color": "$textMuted", "align": "center", "prefix": "✦ ", "suffix": " ✦" },
        { "type": "cta", "y": 1340, "text": "左滑查看更多", "style": "border", "color": "$accent" }
      ]
    },
    "content": {
      "padding": [100, 100, 120, 100],
      "elements": [
        { "type": "pageNum", "y": 60, "font": "$caption", "color": "$accent", "format": "— {n}/{total} —" },
        { "type": "body", "startY": 160, "font": "$body", "color": "$text", "lineHeight": 52, "paragraphGap": 28, "indent": 56, "maxWidth": 880 }
      ],
      "quote": {
        "borderWidth": 3,
        "borderColor": "$accent",
        "font": "italic 26px 'Noto Serif SC', serif",
        "color": "$textLight"
      }
    },
    "ending": {
      "elements": [
        { "type": "emoji", "y": 560, "text": "🍃", "size": 48 },
        { "type": "text", "y": 650, "text": "感谢你的阅读", "font": "bold 40px 'Noto Serif SC', serif", "color": "$text", "align": "center" },
        { "type": "text", "y": 720, "text": "愿你被世界温柔以待", "font": "$body", "color": "$textLight", "align": "center" },
        { "type": "social", "y": 900, "icons": ["like", "collect", "follow"], "color": "$accent" },
        { "type": "cta", "y": 1050, "text": "关注我，获取更多治愈内容", "style": "filled", "color": "$accent" }
      ]
    },

    // === 特殊页面类型（按 form 类型可选） ===
    "list": {
      "itemStyle": "numbered",           // numbered | bullet | emoji | checkbox
      "itemFont": "$body",
      "itemColor": "$text",
      "numberFont": "bold 36px 'Noto Serif SC', serif",
      "numberColor": "$accent",
      "itemGap": 24,
      "maxItemsPerPage": 5
    },
    "comparison": {
      "layout": "side_by_side",          // side_by_side | top_bottom
      "labelA": "Before",
      "labelB": "After",
      "dividerStyle": "vs_circle"        // vs_circle | arrow | line
    },
    "photos": {
      "layout": "grid",                  // grid | collage | hero | masonry
      "columns": 2,
      "gap": 16,
      "radius": 12,
      "overlay": true                    // 图片上叠加文字
    }
  },

  // === AI 辅助匹配 ===
  "matching": {
    "keywords": ["治愈", "语录", "温暖", "感悟", "心灵"],
    "skill": "莫兰迪低饱和配色，衬线字体，留白感强，适合情感类语录和人生感悟",
    "bestFor": ["情感语录", "心灵鸡汤", "读书笔记", "成长感悟"]
  }
}
```

### 3.2 变量引用系统

模板 JSON 中可使用 `$` 前缀引用 palette/typography 中的值，渲染时自动替换：

| 变量 | 映射 |
|------|------|
| `$bg` | `palette.bg` |
| `$text` | `palette.text` |
| `$accent` | `palette.accent` |
| `$title` | `typography.titleFont` |
| `$body` | `typography.bodyFont` |
| `$caption` | `typography.captionFont` |

好处: 用户只改 `palette` 和 `typography`，整套模板配色/字体一键换。

### 3.3 与现有 v1 模板的兼容

| 属性对照 | v1 (当前 CardEngine) | v2 (新模板库) |
|----------|---------------------|---------------|
| 模板定义位置 | JS 硬编码 | JSON 文件 + localStorage |
| 背景渲染 | `_drawBg()` 内 if-else | `background.layers[]` 声明式 |
| 配色 | `palette: {bg, card, text, accent, sub}` | 扩展为 10 色位 |
| 装饰 | 代码中按模板 ID 判断 | `decorations[]` 声明式 |
| 字体 | `serif` + `italic` 布尔值 | 完整 `typography` 对象 |
| 特殊页面 | `hasPhotos` 布尔值 | `pages.list` / `pages.comparison` / `pages.photos` 按需定义 |

**迁移方案**: 
- 新版渲染引擎同时兼容 v1 和 v2 格式
- 内置转换函数 `convertV1toV2(template)` 将旧模板自动升级
- v1 模板标记为 `legacy: true`，UI 上不影响使用

---

## 四、核心功能设计

### 4.1 功能架构

```
┌──────────────────────────────────────────────────────┐
│                   卡片模板库                           │
├──────────┬───────────┬───────────┬───────────────────┤
│ 模板浏览  │ 模板预览   │ 模板编辑器 │ 模板管理          │
│          │           │           │                   │
│ · 分类筛选│ · 实时预览 │ · 配色编辑 │ · 我的模板        │
│ · 标签搜索│ · 多页展示 │ · 字体调整 │ · 收藏夹          │
│ · 风格推荐│ · 示例内容 │ · 布局调整 │ · 导入/导出       │
│ · AI推荐 │ · 对比视图 │ · 装饰元素 │ · 分享链接        │
│ · 瀑布流  │ · 一键套用 │ · 背景层   │ · 版本历史        │
└──────────┴───────────┴───────────┴───────────────────┘
```

### 4.2 模板浏览中心

#### UI 布局

```
┌─────────────────────────────────────────────────┐
│  🎴 卡片模板库              🔍 搜索关键词...     │
├────────┬────────────────────────────────────────┤
│ 筛选   │                                        │
│        │  [精选推荐]  [最新上架]  [最多使用]      │
│ 内容形式│                                        │
│ ○ 全部  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐     │
│ ○ 语录  │  │     │ │     │ │     │ │     │     │
│ ○ 清单  │  │ 预览 │ │ 预览 │ │ 预览 │ │ 预览 │     │
│ ○ 教程  │  │ 图   │ │ 图   │ │ 图   │ │ 图   │     │
│ ○ 对比  │  │     │ │     │ │     │ │     │     │
│ ...    │  ├─────┤ ├─────┤ ├─────┤ ├─────┤     │
│        │  │名称  │ │名称  │ │名称  │ │名称  │     │
│ 视觉风格│  │标签  │ │标签  │ │标签  │ │标签  │     │
│ ○ 全部  │  └─────┘ └─────┘ └─────┘ └─────┘     │
│ ○ 极简  │                                        │
│ ○ 莫兰迪│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐     │
│ ○ 奶油  │  │     │ │     │ │     │ │     │     │
│ ...    │  │ ... │ │ ... │ │ ... │ │ ... │     │
│        │  └─────┘ └─────┘ └─────┘ └─────┘     │
│ 赛道   │                                        │
│ ☑ 情感  │          加载更多...                    │
│ ☑ 美食  │                                        │
│ ...    │                                        │
└────────┴────────────────────────────────────────┘
```

#### 功能细节

| 功能 | 说明 |
|------|------|
| **分类筛选** | 左侧栏按三维度筛选，支持多选，实时刷新结果 |
| **关键词搜索** | 搜索模板名称、标签、bestFor 关键词 |
| **排序方式** | 精选推荐（featured + 权重）/ 最新上架 / 最多使用 / 随机灵感 |
| **瀑布流展示** | 4 列网格（PC）/ 2 列（平板）/ 1 列（手机），懒加载 |
| **缩略图预览** | 展示模板封面页缩略图，hover 显示更多页 |
| **标签气泡** | 模板卡片底部显示 2-3 个标签 |
| **一键收藏** | 右上角心形收藏按钮，保存到个人收藏夹 |

### 4.3 模板详情 & 预览

#### 预览弹窗

```
┌─────────────────────────────────────────────┐
│  ← 返回                    ❤️ 收藏  📥 使用  │
├──────────────────────┬──────────────────────┤
│                      │ 莫兰迪治愈语录        │
│   ┌────────────┐     │                      │
│   │            │     │ 🍃 莫兰迪色 · 语录   │
│   │  封面预览   │     │ 适用: 情感 / 学习     │
│   │  1080×1440 │     │                      │
│   │            │     │ ─────────────────    │
│   └────────────┘     │                      │
│                      │ 色板预览:             │
│  [封面] [内容] [结尾] │ ■ ■ ■ ■ ■ ■          │
│                      │                      │
│  ◄ ●●○ ►            │ 字体: Noto Serif SC   │
│                      │ 风格: 衬线 · 留白     │
│                      │                      │
│                      │ [📥 使用此模板]       │
│                      │ [✏️ 基于此模板修改]    │
│                      │ [📋 导出 JSON]        │
└──────────────────────┴──────────────────────┘
```

#### 功能细节

| 功能 | 说明 |
|------|------|
| **多页切换** | 封面/内容/结尾页轮播预览，示例内容填充 |
| **色板展示** | 直观展示 palette 各色值，点击可复制色号 |
| **示例内容** | 根据模板适用赛道自动填充示例文案 |
| **使用模板** | 一键应用到当前选中的笔记 |
| **基于修改** | 复制一份模板进入编辑器，修改后保存为自定义模板 |
| **导出 JSON** | 下载模板 JSON 文件 |

### 4.4 可视化模板编辑器

> **核心亮点** — 零代码创建自定义卡片模板

#### 编辑器布局

```
┌─────────────────────────────────────────────────┐
│  ✏️ 模板编辑器        [预览] [保存] [导出JSON]    │
├──────────┬─────────────────────┬────────────────┤
│ 属性面板  │                     │ 图层面板       │
│          │    实时预览画布       │                │
│ 🎨 配色  │    1080 × 1440      │ ▼ 背景层       │
│ · 背景色  │                     │   纯色填充     │
│ · 文字色  │   ┌───────────┐     │   纸张纹理     │
│ · 强调色  │   │           │     │   径向渐变     │
│ · ...    │   │  实时渲染  │     │                │
│          │   │  的卡片    │     │ ▼ 装饰层       │
│ 🔤 字体  │   │  预览      │     │   双线边框     │
│ · 标题字体│   │           │     │   角落装饰     │
│ · 正文字体│   └───────────┘     │                │
│ · 字号   │                     │ ▼ 内容层       │
│ · 行高   │  [封面] [内容] [结尾]│   标题         │
│          │                     │   副标题       │
│ 📐 布局  │                     │   正文         │
│ · 内边距  │                     │   页码         │
│ · 对齐   │                     │   CTA按钮      │
│ · 间距   │                     │                │
│          │                     │ [+ 添加元素]   │
│ 🖼️ 背景  │                     │                │
│ · 添加层  │                     │                │
│ · 纹理   │                     │                │
│ · 渐变   │                     │                │
└──────────┴─────────────────────┴────────────────┘
```

#### 编辑能力

| 类别 | 可编辑项 | 交互方式 |
|------|---------|---------|
| **配色** | 10 色位全部可调 | 拾色器 + 预设色板 + 吸管工具 |
| **字体** | 字族、字号、字重、行高、字间距 | 下拉选择 + 滑块 |
| **背景** | 多层叠加: 纯色/渐变/纹理/图案 | 图层排序 + 透明度滑块 |
| **装饰** | 边框/角饰/分割线/散点装饰 | 元素库拖拽添加 |
| **布局** | 内边距/对齐/间距/元素位置 | 数值输入 + 拖拽 |
| **内容结构** | 封面/内容/结尾各元素配置 | 图层面板增删改排序 |

#### 预设色板库

提供 20+ 一键套用的预设配色方案：

```
奶油拿铁 · 莫兰迪绿 · 薄荷巧克力 · 蜜桃乌龙 · 薰衣草 
冰川蓝  · 落日橘  · 樱花粉     · 抹茶      · 焦糖 
经典黑白 · 深海蓝  · 玫瑰金     · 森林绿    · 琥珀 
赛博霓虹 · 多巴胺  · 酸性荧光   · Y2K渐变   · 极光 
```

### 4.5 模板管理

#### 我的模板页

```
┌─────────────────────────────────────────────┐
│  📁 我的模板              [+ 新建] [📥 导入]  │
├─────────────────────────────────────────────┤
│                                             │
│  系统模板 (50)  │  自定义 (3)  │  收藏 (8)   │
│                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐          │
│  │     │ │     │ │     │ │     │          │
│  │     │ │     │ │     │ │     │          │
│  ├─────┤ ├─────┤ ├─────┤ ├─────┤          │
│  │名称  │ │名称  │ │名称  │ │名称  │          │
│  │[编辑]│ │[编辑]│ │[编辑]│ │[编辑]│          │
│  └─────┘ └─────┘ └─────┘ └─────┘          │
│                                             │
└─────────────────────────────────────────────┘
```

| 功能 | 说明 |
|------|------|
| **系统模板** | 预置 50+ 模板，不可删除，可基于修改 |
| **自定义模板** | 用户创建的模板，可编辑/删除/导出 |
| **收藏夹** | 从模板库收藏的模板 |
| **导入** | 支持导入 JSON 文件或粘贴 JSON 字符串 |
| **导出** | 单个导出或批量导出为 JSON |
| **分享链接** | 生成分享链接，他人点击可一键导入 |
| **复制模板** | 复制已有模板，修改后另存为新模板 |

### 4.6 AI 智能功能

| 功能 | 说明 | 实现方式 |
|------|------|---------|
| **AI 匹配推荐** | 根据笔记内容自动推荐最适合的 3 个模板 | 调用 Gemini，传入笔记内容 + 模板列表 |
| **AI 生成模板** | 用自然语言描述想要的风格，AI 生成完整模板 JSON | Prompt: "根据描述生成卡片模板 JSON" |
| **AI 配色建议** | 输入关键词（如"秋天"、"海边"），生成匹配的配色方案 | 调用 Gemini 生成 palette |
| **AI 风格迁移** | 上传一张参考图，AI 提取配色/布局风格生成模板 | Gemini Vision → 分析 → 生成模板 |

#### AI 生成模板示例

**用户输入**: "帮我做一个适合旅行攻略的模板，要有海边日落的感觉，暖色调"

**AI 输出**: 完整的模板 JSON，包含：
- 日落渐变背景（橘红 → 粉紫）
- 暖色调 palette
- 适合攻略的列表布局
- 波浪装饰元素

---

## 五、预置模板库（50+ 模板规划）

### 5.1 系统预置模板清单

按「内容形式 × 视觉风格」组合，首批预置 50 个模板：

#### 语录金句类（8 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 1 | `minimal_quote` | 极简语录 | 极简留白 | 情感/学习 |
| 2 | `morandi_quote` | 莫兰迪语录 | 莫兰迪色 | 情感 |
| 3 | `cream_quote` | 奶油语录 | 奶油风 | 情感/母婴 |
| 4 | `memo_quote` | 便签语录 | 便签风 | 情感/日常 |
| 5 | `letter_quote` | 信纸语录 | 信纸风 | 情感 |
| 6 | `handwrite_quote` | 手写语录 | 手写体 | 情感/日常 |
| 7 | `cyber_quote` | 赛博语录 | 赛博朋克 | 科技/潮流 |
| 8 | `retro_quote` | 国潮语录 | 新中式 | 文化 |

#### 清单列表类（8 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 9 | `minimal_list` | 极简清单 | 极简留白 | 通用 |
| 10 | `memo_list` | 便签清单 | 便签风 | 美食/旅行 |
| 11 | `cream_list` | 奶油清单 | 奶油风 | 家居/美妆 |
| 12 | `magazine_list` | 杂志清单 | 杂志排版 | 穿搭/科技 |
| 13 | `dopamine_list` | 多巴胺清单 | 多巴胺色 | 美食/日常 |
| 14 | `newspaper_list` | 报纸清单 | 报纸风 | 学习/科技 |
| 15 | `glass_list` | 玻璃清单 | 玻璃拟态 | 科技/效率 |
| 16 | `film_list` | 胶片清单 | 胶片复古 | 旅行/穿搭 |

#### 教程步骤类（6 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 17 | `minimal_tutorial` | 极简教程 | 极简留白 | 学习 |
| 18 | `sweet_tutorial` | 甜美教程 | 甜美清新 | 美妆/美食 |
| 19 | `magazine_tutorial` | 杂志教程 | 杂志排版 | 穿搭 |
| 20 | `pixel_tutorial` | 像素教程 | 像素风 | 科技/游戏 |
| 21 | `gradient_tutorial` | 渐变教程 | 渐变流体 | 美妆 |
| 22 | `comic_tutorial` | 漫画教程 | 漫画风 | 日常/手工 |

#### 种草好物类（6 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 23 | `cream_product` | 奶油种草 | 奶油风 | 电商/家居 |
| 24 | `ins_product` | INS种草 | INS美学 | 穿搭/美妆 |
| 25 | `minimal_product` | 极简种草 | 极简留白 | 科技/数码 |
| 26 | `dopamine_product` | 多巴胺种草 | 多巴胺色 | 美食/日用 |
| 27 | `glass_product` | 玻璃种草 | 玻璃拟态 | 科技 |
| 28 | `y2k_product` | Y2K种草 | Y2K千禧 | 潮流/穿搭 |

#### 叙事长文类（6 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 29 | `minimal_story` | 极简长文 | 极简留白 | 情感/成长 |
| 30 | `letter_story` | 信纸长文 | 信纸风 | 情感 |
| 31 | `handwrite_story` | 手写日记 | 手写体 | 日常/情感 |
| 32 | `film_story` | 胶片故事 | 胶片复古 | 旅行 |
| 33 | `morandi_story` | 莫兰迪长文 | 莫兰迪色 | 情感/文化 |
| 34 | `newspaper_story` | 报纸长文 | 报纸风 | 时事/文化 |

#### 排行榜 & 测评类（4 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 35 | `magazine_ranking` | 杂志排行榜 | 杂志排版 | 美食/穿搭 |
| 36 | `cyber_ranking` | 赛博排行榜 | 赛博朋克 | 科技/游戏 |
| 37 | `cream_review` | 奶油测评 | 奶油风 | 美妆/母婴 |
| 38 | `minimal_review` | 极简测评 | 极简留白 | 数码/科技 |

#### 对比 & 问答类（4 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 39 | `magazine_compare` | 杂志对比 | 杂志排版 | 美妆/护肤 |
| 40 | `sweet_qa` | 甜美问答 | 甜美清新 | 母婴/情感 |
| 41 | `glass_compare` | 玻璃对比 | 玻璃拟态 | 科技 |
| 42 | `dopamine_qa` | 多巴胺问答 | 多巴胺色 | 学习/日常 |

#### 食谱 & 攻略类（4 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 43 | `cream_recipe` | 奶油食谱 | 奶油风 | 美食 |
| 44 | `memo_recipe` | 便签食谱 | 便签风 | 美食 |
| 45 | `ins_travel` | INS攻略 | INS美学 | 旅行 |
| 46 | `film_travel` | 胶片攻略 | 胶片复古 | 旅行 |

#### 日签 & 打卡类（4 个）

| # | ID | 名称 | 风格 | 赛道 |
|---|-----|------|------|------|
| 47 | `morandi_daily` | 莫兰迪日签 | 莫兰迪色 | 情感/日常 |
| 48 | `gradient_daily` | 渐变日签 | 渐变流体 | 情感 |
| 49 | `sweet_checklist` | 甜美打卡 | 甜美清新 | 健身/学习 |
| 50 | `retro_checklist` | 国潮打卡 | 新中式 | 文化/健身 |

---

## 六、技术架构

### 6.1 整体架构

```
┌─────────────────────────────────────────────┐
│                 前端 (Browser)               │
│                                             │
│  ┌─────────────┐  ┌──────────────────────┐  │
│  │ CardEngine  │  │ TemplateLibrary      │  │
│  │ v2 渲染引擎 │◄─┤                      │  │
│  │             │  │ · TemplateStore      │  │
│  │ · drawCover │  │ · TemplateEditor     │  │
│  │ · drawBody  │  │ · TemplateSearch     │  │
│  │ · drawEnd   │  │ · TemplatePreview    │  │
│  └─────────────┘  │ · TemplateImporter   │  │
│                   └──────────────────────┘  │
│                          │                  │
│                    ┌─────┴─────┐            │
│                    │ Storage   │            │
│                    │ · localStorage        │
│                    │ · IndexedDB (大模板)   │
│                    └───────────┘            │
│                          │                  │
├──────────────────────────┼──────────────────┤
│                 后端 (server.py)             │
│                          │                  │
│  ┌───────────────────────┴───────────────┐  │
│  │ /api/templates                        │  │
│  │ · GET  /list      → 系统模板列表       │  │
│  │ · GET  /detail/:id → 模板详情         │  │
│  │ · POST /share     → 分享模板          │  │
│  │ · GET  /shared/:code → 获取分享模板   │  │
│  └───────────────────────────────────────┘  │
│                          │                  │
│  ┌───────────────────────┴───────────────┐  │
│  │ templates.db (SQLite)                 │  │
│  │ · system_templates   (系统预置模板)    │  │
│  │ · user_templates     (用户自定义模板)  │  │
│  │ · shared_templates   (分享的模板)      │  │
│  └───────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

### 6.2 存储方案

| 存储位置 | 内容 | 说明 |
|----------|------|------|
| **打包在前端代码中** | 50 个系统预置模板的精简版（仅元信息 + palette + 基本配置） | 保证离线可用、首屏加载快 |
| **server.py + SQLite** | 完整模板数据（含预览图）| 在线时增强体验，支持分享 |
| **localStorage** | 用户自定义模板、收藏列表、最近使用记录 | 客户端持久化，无需登录 |
| **IndexedDB** | 大体积数据（模板预览图 Base64）| localStorage 有 5MB 限制 |

### 6.3 渲染引擎 v2 改动

现有 `CardEngine` 改造为声明式渲染:

```javascript
// 旧方式 (v1): 每个模板需要大量 if-else
_drawBg(ctx, tmpl) {
  if (tmpl.id === 'sweet_fresh') { /* 100行 sweet 专属背景绘制 */ }
  if (tmpl.id === 'dark_cyber') { /* 80行 cyber 专属背景绘制 */ }
  // ...每新增一个模板就要加代码
}

// 新方式 (v2): 数据驱动，通用渲染
_drawBg(ctx, tmpl) {
  for (const layer of tmpl.background.layers) {
    switch (layer.type) {
      case 'solid':    this._fillSolid(ctx, layer); break;
      case 'gradient': this._fillGradient(ctx, layer); break;
      case 'texture':  this._fillTexture(ctx, layer); break;
      case 'pattern':  this._fillPattern(ctx, layer); break;
    }
  }
}
```

需要实现的通用渲染原语:

| 原語 | 方法 | 说明 |
|------|------|------|
| 纯色填充 | `_fillSolid(ctx, {color})` | 单色背景 |
| 线性渐变 | `_fillGradient(ctx, {type:'linear', angle, colors})` | 任意角度渐变 |
| 径向渐变 | `_fillGradient(ctx, {type:'radial', cx, cy, r, colors})` | 中心发散渐变 |
| 纸张纹理 | `_fillTexture(ctx, {texture:'paper_noise', ...})` | 随机噪点 |
| 网格线 | `_fillTexture(ctx, {texture:'grid', gap, color})` | 方格纸 |
| 横线纹 | `_fillTexture(ctx, {texture:'ruled_lines', gap, color})` | 信纸横线 |
| 扫描线 | `_fillTexture(ctx, {texture:'scanlines', gap, opacity})` | CRT效果 |
| 散点装饰 | `_fillTexture(ctx, {texture:'dots', items, emoji})` | emoji散落 |
| 边框 | `_drawDecoration(ctx, {type:'border', style, ...})` | 单线/双线/虚线 |
| 角饰 | `_drawDecoration(ctx, {type:'corner_accent', ...})` | L角/花角 |
| 印章 | `_drawDecoration(ctx, {type:'seal', ...})` | 中式方章/圆章 |
| 波浪线 | `_drawDecoration(ctx, {type:'wave', ...})` | 分割波浪 |
| 文字 | `_drawElement(ctx, {type:'text', ...})` | 通用文字渲染 |
| 标题 | `_drawElement(ctx, {type:'title', ...})` | 标题（自动换行） |
| 正文 | `_drawElement(ctx, {type:'body', ...})` | 正文（段落排版） |
| 列表 | `_drawElement(ctx, {type:'list', ...})` | 有序/无序列表 |
| 图片 | `_drawElement(ctx, {type:'image', ...})` | 图片 cover-fit |
| 分割线 | `_drawElement(ctx, {type:'divider', ...})` | 水平分割线 |
| 页码 | `_drawElement(ctx, {type:'pageNum', ...})` | 页码指示 |
| CTA按钮 | `_drawElement(ctx, {type:'cta', ...})` | 引导互动按钮 |

### 6.4 新增 API 端点

```
GET    /api/templates/list?form=quote&style=morandi&track=emotion&page=1&size=20
GET    /api/templates/:id
POST   /api/templates/share     { template: {...} } → { code: "abc123" }
GET    /api/templates/shared/:code
POST   /api/templates/ai-generate  { description: "..." } → { template: {...} }
POST   /api/templates/ai-palette   { keywords: [...] } → { palette: {...} }
```

---

## 七、交互流程

### 7.1 用户选用模板流程

```
用户点击「生成卡片」
       │
       ▼
  ┌──────────────┐
  │ AI 自动推荐  │──── 满意 ────→ 直接使用
  │ 3 个候选模板 │
  └──────┬───────┘
         │ 不满意
         ▼
  ┌──────────────┐
  │ 打开模板库   │
  │ 浏览/搜索    │
  └──────┬───────┘
         │ 找到想要的
         ▼
  ┌──────────────┐
  │ 预览模板     │──── 直接使用 ────→ 应用到笔记
  │ 查看多页效果 │
  └──────┬───────┘
         │ 想微调
         ▼
  ┌──────────────┐
  │ 进入编辑器   │
  │ 修改配色/字体│
  └──────┬───────┘
         │
         ▼
  保存为自定义模板 → 应用到笔记
```

### 7.2 用户创建模板流程

```
用户点击「新建模板」
       │
       ├─── A. 从零开始 ────→ 选择内容形式 → 选择基础风格 → 进入编辑器
       │
       ├─── B. 基于已有 ────→ 选择一个模板 → 复制 → 进入编辑器
       │
       └─── C. AI 生成 ────→ 输入描述 → AI 生成 → 预览 → 微调 → 保存
```

### 7.3 模板分享流程

```
用户: 导出 JSON → 发给朋友 → 朋友导入 JSON
用户: 生成分享链接 → 发给朋友 → 朋友打开链接 → 一键导入
```

---

## 八、移动端适配

> 平台 60%+ 流量来自移动端，模板库必须移动友好

### 8.1 移动端布局

```
┌─────────────────────┐
│ 🎴 模板库     🔍 ≡  │
├─────────────────────┤
│ [语录][清单][教程]▸  │  ← 横滑标签栏（内容形式）
├─────────────────────┤
│ [极简][莫兰迪][奶油] │  ← 横滑标签栏（视觉风格）
├─────────────────────┤
│ ┌────┐ ┌────┐       │
│ │    │ │    │       │  ← 双列瀑布流
│ │    │ │    │       │
│ ├────┤ ├────┤       │
│ │名称│ │名称│       │
│ └────┘ └────┘       │
│ ┌────┐ ┌────┐       │
│ │    │ │    │       │
│ │    │ │    │       │
│ ├────┤ ├────┤       │
│ │名称│ │名称│       │
│ └────┘ └────┘       │
│                     │
│       加载更多       │
└─────────────────────┘
```

### 8.2 移动端模板编辑器

移动端编辑器采用**底部抽屉 + 标签页**的方式:

```
┌─────────────────────┐
│                     │
│   ┌───────────┐     │
│   │           │     │
│   │  预览画布  │     │  ← 上方: 实时预览
│   │           │     │
│   └───────────┘     │
│                     │
├─────────────────────┤
│ [配色] [字体] [背景] │  ← 底部标签切换
├─────────────────────┤
│ 背景色: [■] #f2ede8 │
│ 文字色: [■] #4a4540 │  ← 当前标签内容
│ 强调色: [■] #8d9f87 │
│ ──────────────────  │
│ 预设色板:           │
│ [奶油] [莫兰迪] ... │
└─────────────────────┘
```

---

## 九、实施路线图

### Phase 1: 基础模板库（2 周）

| 任务 | 预估 | 优先级 |
|------|------|--------|
| v2 模板 JSON Schema 定义 & 解析器 | 2 天 | P0 |
| CardEngine v2 声明式渲染引擎 | 3 天 | P0 |
| v1 → v2 模板自动转换 | 1 天 | P0 |
| 制作 20 个首批预置模板 | 3 天 | P0 |
| 模板浏览页 UI（PC + 移动端）| 2 天 | P0 |
| 模板预览弹窗 | 1 天 | P0 |
| localStorage 存储层 | 0.5 天 | P0 |

**Phase 1 交付**: 用户可浏览 20+ 模板、预览、选用、替换现有 15 个硬编码模板。

### Phase 2: 自定义 & 导入导出（1.5 周）

| 任务 | 预估 | 优先级 |
|------|------|--------|
| 可视化模板编辑器（配色/字体/布局）| 3 天 | P1 |
| 模板 JSON 导入/导出 | 1 天 | P1 |
| 收藏夹功能 | 0.5 天 | P1 |
| 再制作 30 个预置模板（达到 50 个）| 3 天 | P1 |
| 模板搜索 & 筛选 | 1 天 | P1 |
| 编辑器移动端适配 | 1.5 天 | P1 |

**Phase 2 交付**: 用户可自定义模板、导入导出、收藏。50 个预置模板就位。

### Phase 3: AI 增强 & 特殊页面（1 周）

| 任务 | 预估 | 优先级 |
|------|------|--------|
| AI 模板生成（描述 → JSON）| 2 天 | P2 |
| AI 配色建议 | 1 天 | P2 |
| 特殊页面类型（列表/对比/时间线）| 2 天 | P2 |
| AI 风格迁移（参考图） | 2 天 | P2 |

**Phase 3 交付**: AI 生成 & 匹配全面增强，支持更多内容形式。

### Phase 4: 社区 & 分享（1 周）

| 任务 | 预估 | 优先级 |
|------|------|--------|
| 后端模板 API + 数据库 | 2 天 | P3 |
| 分享链接生成 & 导入 | 1 天 | P3 |
| 模板使用统计 & 热门排序 | 1 天 | P3 |
| 模板版本历史 | 1 天 | P3 |

**Phase 4 交付**: 用户间可分享模板、社区热门排行。

---

## 十、成功标准

| 指标 | 目标 | 衡量方式 |
|------|------|---------|
| 模板总数 | ≥ 50 个系统预置 | 清点模板文件 |
| 风格覆盖 | ≥ 20 种视觉风格 | 维度 B 去重计数 |
| 赛道覆盖 | 7 大赛道每个至少 5 个匹配模板 | 按 tracks 筛选 |
| 加载性能 | 模板列表首屏 < 500ms | 性能埋点 |
| 渲染性能 | 单页卡片渲染 < 100ms | 性能埋点 |
| 编辑器可用性 | 创建自定义模板平均耗时 < 3 分钟 | 用户测试 |
| 用户满意度 | 模板选用率 > 80% | 使用统计 |

---

## 附录 A: 纹理 & 装饰引擎渲染原语一览

```
纹理层 (texture) 支持:
├── paper_noise      纸张噪点
├── canvas_grain     画布纹理
├── linen            亚麻布纹
├── grid             方格网
├── ruled_lines      横线（信纸/笔记本）
├── dots_grid        圆点网格
├── diagonal_lines   斜线
├── scanlines        CRT扫描线
├── film_grain       胶片颗粒
├── bokeh            散景光斑
├── emoji_scatter    Emoji散落
└── noise_gradient   渐变噪波

装饰层 (decoration) 支持:
├── border           边框（solid/double/dashed/dotted）
├── corner_accent    角饰（L形/花角/折角）
├── seal             印章（方/圆/椭圆）
├── wave             波浪线
├── ribbon           丝带横幅
├── tape             胶带条
├── torn_edge        撕纸边缘
├── film_holes       胶片孔
├── cloud            云朵
├── star             星星
├── heart            心形
└── custom_svg       自定义 SVG 路径
```

## 附录 B: 入口位置建议

模板库作为独立功能，建议在以下位置放置入口:

| 入口位置 | 触发场景 | 说明 |
|----------|---------|------|
| 侧边栏「🎴 模板库」| 主动浏览 | 一级功能入口 |
| 卡片生成弹窗「更换模板」| 生成卡片时 | 当前模板不满意时快速切换 |
| 设置页「卡片模板管理」| 管理模板 | 导入/导出/删除/编辑 |
| AI 生成结果页底部 | 生成完成后 | "不喜欢这个风格？→ 换个模板" |

---

> 📝 文档版本: v1.0  
> 📅 日期: 2026-03-16  
> 👤 作者: GitHub Copilot  
> 🔄 下次更新: Phase 1 技术详设完成后
